<div class="signup-h">
  <h3><%= space.name %></h3>
</div>
<div class="backplate">
  <div class="overflow">
    <img class="spaces-img" src="<%= space.image %>" />
    <p><%= space.contactName %></p>
    <p><%= space.homepage %></p>
    <form action="/spaces/<%= space._id %>" method="post">
      <button class="button" type="submit">Send a Request</button>
    </form>
  </div>
</div>